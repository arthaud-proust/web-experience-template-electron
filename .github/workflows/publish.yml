name: Publish

on:
  push:
    branches: ["feature/publish"]

jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout latest commit
        uses: actions/checkout@v4

      - name: Make packages
        run: |
          npm ci
          npm run make
